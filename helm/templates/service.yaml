apiVersion: v1
kind: Service
metadata:
  name: {{ include "rewind.fullname" . }}
  labels:
    {{- include "rewind.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rewind.selectorLabels" . | nindent 4 }}
